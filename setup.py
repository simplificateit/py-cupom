from distutils.core import setup
setup(
  name = 'py_cupom',
  packages = ['py_cupom'], # this must be the same as the name above
  version = '0.1',
  description = 'A python script to generate temporary codes with low collision rate.',
  author = 'AlexVanAxe',
  author_email = 'alexvanaxe@gmail.com',
  url = 'https://github.com/simplificateit/py-cupom', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/tarball/0.1', # I'll explain this in a second
  keywords = ['cupom', 'generator', 'random'], # arbitrary keywords
  classifiers = [],
)